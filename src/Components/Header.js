import React from 'react'

const Header = () => {
    return (
        <div>
            <h1>Assignment Organizer</h1>
            <h2>Keep track of when assignments are due to stay on top of your studies and never lose track again</h2>
        </div>
    )
}

export default Header
